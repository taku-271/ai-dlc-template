# [機能名] 実装ドラフト

<!-- spec.md を読んだ上で、ファイル名・メソッド名レベルまで特定した実装計画を記述する -->
<!-- 抽象的な方針合意のままAIに実装させると「典型的なパターン」に引き寄せられる -->
<!-- このドラフトでAIの解釈余地をなくす -->

## 影響を受ける既存ファイル

| ファイルパス | 変更種別 | 変更内容 |
|---|---|---|
| `src/...` | 追加 / 変更 / 削除 | |

## 新規作成ファイル

| ファイルパス | 役割 |
|---|---|
| `src/...` | |

## 主要なメソッド・関数定義

```typescript
// 例：ファイルパスとシグネチャを明示する
// src/server/routers/xxx.ts
export const xxxRouter = router({
  create: protectedProcedure
    .input(z.object({ name: z.string() }))
    .mutation(async ({ input, ctx }) => { ... })
})
```

## 実装順序

1. 型定義・Schema
2. （以降を記述）

## 既知の落とし穴・注意事項

- （実装前に把握している制約・注意点）

## Material Ambiguity（実装前に確認が必要な曖昧性）

<!-- 以下のいずれかに影響する不確実さを記録する -->
<!-- 実装の方向・検証・ロールアウト・データ処理・権限・ユーザー可視動作 -->

- [ ] （確認済みにしたら spec.md を更新すること）
