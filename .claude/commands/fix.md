# バグ修正フロー

引数としてバグの概要を受け取ります（例: `/fix ログイン後にリダイレクトされない`）。

spec.md は作成しません。「原因特定 → 修正計画の承認 → 失敗テスト作成 → 修正」の順で進めます。

---

## Step 1: 原因調査

以下を読んでバグを調査してください：
- `CLAUDE.md`（技術スタックの確認）
- `docs/inception/domain-model.md`（用語・エンティティの確認）
- バグに関係するソースファイル

調査完了後、以下を報告して必ず停止してください：

「🔍 調査結果を報告します。
- **再現条件**: （どうすれば発生するか）
- **根本原因**: （なぜ起きているか）
- **影響範囲**: （他に影響しそうな箇所）
- **修正方針**: （どう直すか）

続けてよろしいですか？」

**承認があるまで次のステップに進まないこと。**

---

## Step 2: 修正計画の提示（承認をもらってから実行）

コードを書く前に以下を提示して承認を得てください：
- 変更するファイルの一覧
- 特記事項（リグレッションリスクがある箇所など）

**承認を得てから実装に進むこと。**

---

## Step 3: 失敗テストの作成（承認をもらってから実行）

**⚠️ テストを作成するまで、修正コードを1行も書かないこと。**

バグを再現するテストを先に書いてください。

- 既存のテストファイルに追加するか、新規ファイルを作成するか判断する
- テストはバグの再現条件を正確に表現すること

テストを作成したら実行して **RED（失敗）** であることを確認してください。

完了後、以下のメッセージを出力して必ず停止してください：

「✅ 失敗テストを作成しました。テストを実行してバグが RED で再現されていることを確認してください。確認できたら「テストOK」とお伝えください。」

**「テストOK」という返答があるまで修正コードを1行も書かないこと。**

---

## Step 4: 修正の実装（「テストOK」をもらってから実行）

テストが GREEN になるように修正してください。

- 修正はバグの根本原因に対して行う（症状だけを隠す workaround にしない）
- `docs/inception/security-policy.md` のルールを守ること
- 既存テストが壊れていないか確認すること

---

## Step 5: 完了報告（Final Report）

以下の形式で報告してください：

| 項目 | 内容 |
|---|---|
| 変更概要 | 何を修正したか |
| 変更ファイル | 影響範囲（追加・変更） |
| テスト結果 | 追加テスト件数・全体の結果（GREEN/RED） |
| リスク・懸念点 | リグレッションリスク・副作用 |
| 関連する問題 | 調査中に発見した別の問題（あれば） |
| 未完了事項 | 残タスク（あれば） |
